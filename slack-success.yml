username: Digdag
icon_emoji: ':blush:'
attachments:
- fallback: '[SUCCESS] ${workflow_name} workflow'
  color: "good"
  text: '*[SUCCESS]* `${workflow_name}` Workflow'
  mrkdwn_in:
  - text
  - pretext
  - fields
  fields:
  - title: Session Date
    value: "${session_date}"
    short: true
  - title: Syncing Table
    value: "${syncing_table}"
    short: true
